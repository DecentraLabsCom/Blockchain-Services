# Example Environment Variables for Blockchain Services
# Copy this file to .env and fill in your actual values
# NEVER commit .env file to version control!

# ========================================
# CRITICAL BLOCKCHAIN CONFIGURATION
# ========================================
# Smart contract address
CONTRACT_ADDRESS=0xYourContractAddress

# Blockchain RPC endpoint
RPC_URL=https://your-rpc-endpoint

# ========================================
# INSTITUTIONAL WALLET CONFIGURATION
# ========================================
# SETUP (one-time):
# 1. Start the service: docker-compose up -d
# 2. Create institutional wallet: 
#    curl -X POST http://localhost:8080/wallet/create \
#      -H "Content-Type: application/json" \
#      -d '{"password":"YourSecurePasswordHere"}'
# 3. Copy the returned address to INSTITUTIONAL_WALLET_ADDRESS below
# 4. Set INSTITUTIONAL_WALLET_PASSWORD to the password you used
# 5. Restart the service: docker-compose restart

# Institutional wallet address (from wallet creation endpoint)
# This wallet will be used for all automated blockchain transactions
INSTITUTIONAL_WALLET_ADDRESS=0xYourInstitutionalWalletAddress

# Password to decrypt the institutional wallet
# SECURITY: Use strong password, store in secret manager (AWS Secrets Manager, Azure Key Vault)
# NEVER commit this password to version control!
INSTITUTIONAL_WALLET_PASSWORD=YourSecurePasswordHere

# ========================================
# WALLET ENCRYPTION
# ========================================
# Salt for wallet encryption (change from default!)
# Generate with: openssl rand -base64 32
WALLET_ENCRYPTION_SALT=YourRandomSaltHere-ChangeThis!

# ========================================
# WALLET PERSISTENCE (PRODUCTION)
# ========================================
# Enable persistent storage of encrypted wallets (REQUIRED for institutional wallet)
# false = in-memory only (institutional wallet will NOT work)
# true = saved to file (survives restarts, REQUIRED)
WALLET_PERSISTENCE_ENABLED=true

# Path to wallet storage file (inside container)
WALLET_FILE_PATH=/app/data/wallets.json

# ========================================
# NETWORK-SPECIFIC RPC URLS
# ========================================
# Mainnet RPC (with your API key)
ETHEREUM_MAINNET_RPC_URL=https://mainnet.infura.io/v3/YOUR_PROJECT_ID

# Sepolia testnet RPC (with your API key)
ETHEREUM_SEPOLIA_RPC_URL=https://sepolia.infura.io/v3/YOUR_PROJECT_ID

# ========================================
# JWT KEY PATHS (optional overrides)
# ========================================
# PRIVATE_KEY_PATH=/app/config/keys/private_key.pem
# PUBLIC_KEY_PATH=/app/config/keys/public_key.pem
# PUBLIC_CERTIFICATE_PATH=/app/config/keys/certificate.pem

# ========================================
# ADDITIONAL CONFIGURATION (optional)
# ========================================
# SPRING_PROFILES_ACTIVE=prod
# ALLOWED_ORIGINS=https://your-domain.com
